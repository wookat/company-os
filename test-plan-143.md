# UX 第 143 轮 — 提醒/邮件成长闭环体验走查（生产 Worker 0c4f799a，非功能 QA）

部署核对（已完成）：app2 bundle `assets/index-CICgmaFE.js` 含新注册超时文案「注册请求可能已在服务端完成」×1。
代码依据：
- app2 提醒卡：web/src/pages/Account.tsx L153-157，文案「每天 8:00 发邮件提醒：到期错题数 + 每日一题；当天已打卡则不打扰」。
- 旧版提醒卡：public/app.html L2384-2387，文案「每天 8:00 邮件提醒到期错题与每日一题，当天已打卡不发送」——双端口径已可见轻微差异（走查确认视觉呈现）。
- 引导 toast：Home.tsx L264-271（3.5s 后出、无跳转、纯文本）；toast 显示时长 3000ms（store.tsx L49）。
- 注册慢路径：api.ts L27 AbortController 20s 超时 → ApiError(status 0)；Auth.tsx L61-66 仅 register 模式弹新文案 toast（同样 3s 时长）。
账号：ux143-<ts>@test.zalize.com（app2 注册，用于双端走查；报邮箱+uid）。

## W1 双端提醒开关动线走查（1440px + 390px）
- app2 /app2/#account 与旧版 /app#account 各截图：开关卡的位置（滚动深度）、标题/副文案逐字对比、switch 视觉（尺寸/品牌色）。
- 评估点：位置是否好找（需滚动几屏？）、双端文案口径是否一致、开启后用户对「明早 8:00 收到什么」的预期是否清楚（副文案是否讲明内容+豁免）、开启 toast 是否补充预期。
- 输出：一致/不一致清单 + P 级意见。

## W2 打卡→引导 toast→开提醒转化动线
- 新号打卡实测引导 toast（复用 QA142 已知：3.5s 后出现、显示 3s、不可点击、无跳转）。本轮从转化视角计时评估：3 秒内用户需读完 22 字并理解「我的」页在哪——按导航层级走一遍「工作台→我的→开关」数点击数与滚动深度。
- 评估点：toast 时长 vs 文案长度（阅读速度按 ~8字/秒）、是否可点击、消失后无任何再触达（一次性）。给出改进建议（可点击跳转 #account / 延长时长 / 改 banner 等），不改代码。

## W3 注册慢路径新提示
- 未登录 /app2/#reg，console 包装 fetch 仅对 `/api/register` 延迟 25s（超 20s abort），填 ux143 新邮箱点「注册并开始刷题」：
  - 预期 ~20s 后 toast 逐字「网络较慢，注册请求可能已在服务端完成——请稍后用该邮箱密码直接登录，若提示不存在再重新注册」（截图）。
  - 评估：可读性（38 字 vs 3s 时长！）、可行动性（用户下一步明确吗）、390px 下是否折行溢出（CDP 390 复测一次）。
  - 注意：服务端实际会收到该次注册（延迟包装在客户端 resolve 前仍已发出？——包装为「延迟后才发真实请求」则服务端不建号；用「真实请求照发、response 延迟 25s」的包装，符合真实慢网语义，账号会建成，之后直接登录即可）。
- 对照负例：登录模式同样超时应走旧文案「网络较慢或已超时，请重试」（可选，时间允许时验）。

## W4 回归（顺带）
- 两端「我的」页 390px/1440px 视觉层级截图（W1 已覆盖大半）；scrollWidth=390 无溢出。
- console/pageerror 全零（豁免：W3 计划内注入、登录前 401）。

产出：test-report-143.md（P0-P3 问题清单 + 建议 + 截图）。执行顺序：注册 ux143（记 uid）→ W2（新号首打卡）→ W1 双端 → W3 → W4 汇总。
