# 批次A·竞品对标 6 项生产回归（build 122843b，bundle assets/index-DBDu9E94.js 已上线）

前提（已核实）：bundle 已含全部 6 项特征串（1-4 可快速作答/觉得答案或解析有误/未来 7 天待复习分布/ztpop/蒙对/犹豫）。代码依据：Exam.tsx L255-262（numMap 1-4→A-D、Enter/→ 下一题、TEXTAREA 豁免）、L189+227（submit 带 hesitated: marks）、src/index.js L1844-1847（答对但 hesitated 也入 wrong_book，your_answer=所选答案）、Result.tsx FlagLink（POST /api/questions/:id/flag→「已反馈，感谢帮助我们改进题库」，客观题+分析题两处）、Wrong.tsx L96-109+L195-214（dueDist 7 桶柱图，今日=rose 柱，仅 due 数据存在时显示）、index.css .pop（ztpop 0.15s scale1.12）、src/index.js shuffleOptions L221-236（仅 AI 生成题落库前打乱）。新注册 qa154 号（报 uid 清库）。

## T1 键盘快捷键（桌面 1440，录屏）
真题区 #real 按年份 → 2025 整卷模考（33 题，免费）：
1. 第 1 题按键盘 `2`：断言选项 B 高亮选中（单选 autoNext 默认开则自动跳下一题——先在页脚关掉自动跳转以便观察，或直接以「B 选中且进入下一题」为通过口径）。
2. 按 `→` 与 `Enter` 各一次：题号各 +1；按 `←`：题号 -1。
3. 多选题提示文案显示「键盘 A-D / 1-4 可快速作答，回车/→ 下一题」。
失败判据：按 1-4 无选中 / 回车不翻题。
（TEXTAREA 豁免不专测：真题客观卷无 essay 题，标注 untested。）

## T2 犹豫标记入错题本（同一份 2025 卷）
1. 挑 2 题查好正确答案后**答对**并点「标记待查”（点亮瞬间观察 pop 微动效→T5）。
2. 其余题随意作答（制造若干真错题），交卷（确认弹窗提示 N 题标记待查）。
3. 错题本：断言这 2 道答对+标记的题出现在错题列表，且展开后「你的答案」=所选正确答案（绿色/等于 answer）。
失败判据：答对标记题未入错题本，或 your_answer 为空。

## T3 解析纠错入口（成绩页）
1. 交卷后成绩页逐题解析：任一题解析卡底部有灰色虚线「觉得答案或解析有误？反馈 ›」。
2. 点击 → 变绿色「已反馈，感谢帮助我们改进题库」（截图前后）。
3. 分析题：#real 分析题区打开任一篇作答提交（或复用已有解析卡）——若分析题成绩页同样有 FlagLink 则核一处；不可达则标 untested 并说明。
失败判据：无链接 / 点击后无状态变化或 HTTP 错误。

## T4 错题本 7 天分布柱图
1. T2 交卷后错题本头部应出现「未来 7 天待复习分布」白卡，「今日」桶=rose 色且计数=当前到期错题数。
2. 点「错题重练」把 2-3 题答对（box1 答对 due+1 天）→ 刷新错题本：断言 +1天 桶出现相应计数、今日桶减少。
失败判据：卡不显示 / 重练后分布不变。

## T5 微动效（pop）
1. 答题页「标记待查」点击瞬间与错题本「收藏」点亮瞬间：连拍/录屏帧观察 scale 弹一下；同时 CDP 断言点亮态 class 含 `pop` 且 computedStyle animationName=ztpop。
判定口径：class+animation 存在 = passed（150ms 视觉帧难以静态截图，以录屏+DOM 双证）。

## T6 AI 出题选项随机化（P0 回归重点）
1. 用 qa154 走 QA151 路径：真题 2 题考点卷 0/2 → AI 补练 → material 页 5 题快练生成。
2. 生成后原卷答题页或成绩页取 5 题正确答案分布：断言不全部集中同一字母（5 题全同判 failed；注：shuffle 随机仍可能偶然聚集，若 4/5 同字母则再生成一份用模拟卷额度复核）。
3. **一致性人工核对 3-5 题**：逐题读题干/四选项/答案字母/解析——解析支持的选项内容必须=答案字母指向的选项内容（随机化错位即 P0）。
失败判据：任一题解析与答案字母指向的选项内容矛盾。

## T7 常规回归
- 390px 与 1440px 各抽 1 屏（答题页/错题本柱图）无溢出。
- CDP 监听全程 console error / pageerror / HTTP≥400 = 0（豁免计划内探测）。
- 核心流程 登录→做卷→交卷→成绩→错题本 全程即 T1-T4 覆盖。

产出：test-report-154.md + 录屏 + qa154 uid。
