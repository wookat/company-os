# 第 152 轮 — UX 走查：新考点索引 + 按考点 chip 列表可用性 + 额度 toast 复验（build 3c89b0c）

前提（已核实）：部署 bundle `app2/assets/index-CmWlq5YR.js` 含新 toast 文案「今日快练已用完，可改选 10 题走模拟卷额度」与「今日模拟卷已用完，可改选 5 题快练」（web/src/pages/Material.tsx L65-72，客户端在 POST 前按 /api/me quota 预检）；qa151 账号已被清库，T3 需新注册 qa152 号并先烧掉快练额度。

## W1 /zhenti/kaodian 新考点 UX 走查（公开页，1440+390）
- 5 个新考点在科目分组中的位置：抗美援朝/毛泽东思想的形成与发展 应在史纲/毛中特组、道德的本质与功能/中华传统美德 在思修组、习思想 在毛中特（或习思想）组——核对分组是否正确、组内排序（题数序/名称序）是否与相邻协调、命名口径（无「的」字/长度）是否突兀。
- 点进「抗美援朝」与「中华传统美德」考点页：标题 H1、速查表（考点释义）、题目清单、CTA（去刷题/AI 补练）与老考点页（如「抗日战争」）同构一致；无缺块。
- 首页考点数口径显示 119（build 3c89b0c 主变更）——打开 https://zhenti.zalize.com/ 断言页面显示「119」而非 114。

## W2 app2 真题区「按考点」chip 列表可用性评估（登录态，1440+390）
- 量测（CDP）：chip 实际高度/垂直间距/热区（断言口径：热区≥32px 为可接受、<28px 记 P2）；390px 下单屏可见考点数与总考点数。
- 任务法：以真实用户视角找指定考点「量变质变规律」（马原组内），记录所需滚动屏数与用时；评估有无科目分组锚点/搜索过滤辅助（Real.tsx L123 按考点 tab、L150 搜索框是否覆盖考点名检索）。
- 输出 P2/P3 分级建议（不改代码）。

## W3 额度 toast 复验（新号 qa152-<ts>@test.zalize.com）
1. 注册后进任一官方考点库资料页（AI 补练入口或直接 kpdrill 弱项路径太长——改走：真题区做一个 2 题考点卷 0/2 → AI 补练 → material 页），选 5 题快练生成一次（烧掉 quick 额度，卷子生成即可不作答）。
2. 回 material 页再选 5 题点「生成仿真模拟卷」：断言 toast 逐字=「今日快练已用完，可改选 10 题走模拟卷额度」（不再静默）。
3. 不消耗模拟卷额度（保持 paper_left=1 以验证该分支文案）。
失败判据：点击仍静默无反馈，或文案不符。

## W4 常规回归
- CDP 监听 console/pageerror/HTTP≥400 = 0（豁免计划内 402/探测）。
- 390px 抽查 /zhenti/kaodian 无横向溢出（scrollWidth=390）。

产出：test-report-152.md + 录屏（W2/W3 UI 部分）+ P 级清单 + 测试号 uid 报清库。
