# QA155 · 增量回归（build 153251f，bundle assets/index-Cu90KbVZ.js 已含「展开完整解析」串）

代码依据：
- Result.tsx L32-51 `Analysis`：`t.length>90` 且首句正则 `^.{8,}?[。；!！?？]` 命中且有余文 → 首句 `font-medium`（加粗）+「展开完整解析 ▾」按钮，点击 setOpen(true) 显示 rest；否则原样 `<p>` 显示。仅客观题分支（L376）使用；essay 分支（L339）仍原样。
- src/index.js 021e0df：`shuffleOptions(options, answer, analysis)` 洗牌后用两段式替换重写 analysis 中孤立的 A-D 字母（含 BCD 连续组合重排序），落库 s.analysis。仅对新生成 AI 题生效。
- Exam.tsx L355-358：标记待查 onClick 后 `blur()`。

数据事实：全库唯一「不折叠」客观题 = 2010 卷 seq 20（analysis 294 字但整段一句，rest 为空 → 原样显示）；2025 等卷客观题解析全部满足折叠条件。

新注册 qa155 号（报 uid 清库）。全程录屏 + CDP 监听 console/pageerror/HTTP≥400。

## T1 一句话解析置顶（真题 2010 整卷）
1. #real 按年份 → 2010 整卷模考。答错前几题（全选 A 等），确保 seq 20 也作答（任意），提前交卷。
2. 成绩页逐题解析（长解析题，如 Q1）：
   - passed 判据：解析首句单独一行加粗（font-medium 深色），下方蓝色小字按钮「展开完整解析 ▾」，其余文字默认不可见。
   - 点按钮 → 其余解析出现，按钮消失。
3. 短/单句解析题（Q20）：解析整段原样显示，无加粗首句拆分、无「展开完整解析」按钮。
4. 390px（CDP 模拟）：折叠卡无溢出（scrollWidth=390），按钮可见。
失败判据：长解析未折叠 / 点击不展开 / Q20 出现按钮或文字被截断。

## T2 AI 解析字母重写复验（P1 修复）
1. 真题区按考点 → 「抗美援朝 2 题」组卷 0/2 → 弱项榜「AI 补练 ›」→ material 页 10 题模拟卷生成（新卷=新 shuffle 路径）。
2. 全答 A 交卷 → 成绩页逐题：对全部 10 题核对「✓ 正确答案」字母 vs 解析中「X 正确 / X 错误」字母，必须一致（QA154 时 7/10 矛盾）。
3. 抽 3 题人工核对语义：答案字母指向的选项内容 = 解析论证支持的内容。
失败判据：任一题解析字母与显示答案矛盾，或字母指向的选项内容与解析语义不符。

## T3 标记待查失焦复验（P3 修复）
1. 任一答题页（可用 T2 的 AI 卷或免费快刷卷）第 1 题点「标记待查」（点亮）。
2. 直接按 Enter ×2：
   - passed 判据：题号变为 3（Enter 翻题），且第 1 题标记保持点亮、无新增标记（答题卡待查数=1）。
   - QA154 旧行为：Enter 反复 toggle 标记、题号不动。
失败判据：Enter 后仍停留第 1 题或标记被 toggle。

## T4 常规
- CDP 监听全程 console error / pageerror / HTTP≥400 = 0。
- 核心链路（登录→做卷→交卷→成绩）由 T1/T2 覆盖。

产出：test-report-155.md + 录屏 + qa155 uid。
